# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*
!.env.example
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# ---- R ----
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj
# renv dirs — we don't currently use renv (removed in favour of direct user-
# library install), but ignore its scaffolding if a future branch reintroduces it.
renv/library/
renv/staging/
renv/python/
renv/local/
renv/sandbox/
R/renv/library/
R/renv/staging/
R/renv/python/
R/renv/local/
R/renv/sandbox/
.Renviron
*.rds

# ---- MLflow ----
mlruns/
mlartifacts/

# ---- Raw data — regenerable, do NOT commit ----
data/raw/lending_club_sample_50k.parquet
data/raw/macro.parquet
data/raw/accepted_*.csv*
data/raw/rejected_*.csv*
data/raw/lending-club.zip

# ---- Derived feature parquets (regenerable via pipeline/ingest.py) ----
data/features/

# ---- Claude Code local settings ----
.claude/settings.local.json

# ---- Large model binaries (joblibs / pkls are regenerable) ----
*.joblib
*.pkl
models/*/artifacts/*.joblib
models/*/artifacts/*.pkl

# ---- Large artifact parquets (regenerable — small .md / .json / .png reports are committed) ----
**/artifacts/*.parquet

# ---- Top-level scratch artifacts dir (e.g. artifacts/ecl) ----
/artifacts/

# ---- DB volumes ----
postgres_data/

# ---- Logs ----
*.log

# ---- OS ----
.DS_Store
Thumbs.db
desktop.ini

# ---- IDE ----
.idea/
.vscode/
*.swp
*~
